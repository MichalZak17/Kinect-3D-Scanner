# Full requirements (client + server combined).
# For individual installs, see requirements-client.txt and requirements-server.txt.

numpy
open3d
opencv-python
PyQt6
trimesh
httpx
websocket-client
fastapi
uvicorn[standard]

# freenect — not on PyPI, must be built from source:
#   git clone https://github.com/OpenKinect/libfreenect
#   cd libfreenect && mkdir build && cd build
#   cmake .. -DBUILD_PYTHON3=ON -DCMAKE_INSTALL_PREFIX=/usr
#   make && sudo make install
#   cd ../wrappers/python && pip install .
